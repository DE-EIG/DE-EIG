function U = xover(X, V, U, CR, D, P, NP)
% Eigendecomposition (14)
[Q, ~] = eig(cov(X'));

% Crossover (16)
for i = 1 : NP
	U(:, i) = mixcrossover(...
		X(:, i), ...
		V(:, i), ...
		U(:, i), ...
		CR(i), ...
		D, ...
		Q, ...
		P);
end
end

